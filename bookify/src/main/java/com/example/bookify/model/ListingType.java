package com.example.bookify.model;

/**
 * Enum representing different types of listings in the system.
 * This enum defines the types: HOTEL, FLIGHT, and EVENT.
 */
public enum ListingType {
    HOTEL,
    FLIGHT,
    EVENT
}
